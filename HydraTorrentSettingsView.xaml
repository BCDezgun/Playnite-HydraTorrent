<UserControl x:Class="HydraTorrent.HydraTorrentSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="600">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="15">
            <TextBlock Text="Настройки qBittorrent" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

            <CheckBox x:Name="chkUseQb" Content="Использовать qBittorrent для скачивания" 
                      IsChecked="{Binding Settings.UseQbittorrent}" Margin="0,0,0,15"/>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Адрес:" VerticalAlignment="Center" Width="120"/>
                <TextBox Grid.Column="1" Text="{Binding Settings.QBittorrentHost}" />
            </Grid>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Порт:" VerticalAlignment="Center" Width="120"/>
                <TextBox Grid.Column="1" Text="{Binding Settings.QBittorrentPort}" />
            </Grid>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Логин:" VerticalAlignment="Center" Width="120"/>
                <TextBox Grid.Column="1" Text="{Binding Settings.QBittorrentUsername}" />
            </Grid>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Пароль:" VerticalAlignment="Center" Width="120"/>
                <PasswordBox Grid.Column="1" x:Name="txtPassword" PasswordChanged="txtPassword_PasswordChanged" />
            </Grid>

            <Separator Margin="0,10"/>
            <TextBlock Text="Пути загрузки" FontWeight="Bold" FontSize="16" Margin="0,10,0,10"/>

            <CheckBox Content="Использовать путь по умолчанию для всех загрузок" 
                      IsChecked="{Binding Settings.UseDefaultDownloadPath}" Margin="0,0,0,10"/>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Путь загрузки:" VerticalAlignment="Center" Width="120"/>
                <TextBox Grid.Column="1" Text="{Binding Settings.DefaultDownloadPath}" x:Name="DefaultPathText" />
                <Button Grid.Column="2" Content="Обзор..." Click="BrowseDefaultPath_Click" Margin="5,0,0,0" Padding="10,2"/>
            </Grid>

            <Button Content="Протестировать подключение к qBittorrent" 
                    Click="TestConnection_Click"
                    HorizontalAlignment="Left"
                    Padding="20,8"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>