<UserControl x:Class="Playnite_HydraTorrent.Views.DownloadProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="48" d:DesignWidth="200"
             Visibility="Collapsed">

    <Grid ClipToBounds="False">
        <!-- Particles canvas (behind button) -->
        <Canvas x:Name="ParticlesCanvas" Margin="-10,-10,-10,-10"
                IsHitTestVisible="False" Panel.ZIndex="1"/>

        <Button x:Name="DownloadBtn" Height="48" FontWeight="Bold"
                Background="Transparent" BorderBrush="Transparent"
                Cursor="Hand" Click="DownloadBtn_Click"
                Panel.ZIndex="2">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- Base background (opaque) -->
                        <Border CornerRadius="24" Background="#1A1A1A"/>

                        <!-- Progress fill -->
                        <Border x:Name="ProgressBorder" CornerRadius="24"
                                HorizontalAlignment="Left" Width="0">
                            <Border.Background>
                                <SolidColorBrush x:Name="ProgressBrush" Color="#4CAF50"/>
                            </Border.Background>
                        </Border>

                        <!-- Outer border -->
                        <Border CornerRadius="24" BorderBrush="#555" BorderThickness="1"/>

                        <!-- Text -->
                        <TextBlock x:Name="StatusText" Text="Загрузка: 0%"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontSize="12" FontWeight="Bold" Foreground="White"/>
                    </Grid>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>
